/*
    🐺 LXR-SPIRITS | wolves.land | The Lux Empire
    NUI controller for ritual, profile, reveal, and omen screens.
*/
const app=document.getElementById('app'),content=document.getElementById('content'),closeBtn=document.getElementById('closeBtn'),title=document.getElementById('title'),subtitle=document.getElementById('subtitle');let locale={};function post(n,d={}){fetch(`https://${GetParentResourceName()}/${n}`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(d)}).catch(()=>{})}function show(){app.classList.remove('hidden')}function hide(){app.classList.add('hidden');app.classList.remove('pulse')}function esc(v){return String(v??'').replace(/[&<>\"]/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;'}[m]))}function setBase(e){locale=e.locale||locale;title.textContent=(e.brand&&e.brand.title)||locale.title||'LXR-SPIRITS';subtitle.textContent=(e.brand&&`${e.brand.server} • ${e.brand.company}`)||locale.subtitle||'wolves.land • The Lux Empire';closeBtn.textContent=locale.close||'Close'}function spiritBlock(d){const s=d.spirit||d,omen=d.omen;return `<div class="spirit">${esc(s.icon||'◆')} ${esc(s.label||d.label||d.spirit||'Unknown')}</div><div class="title">${esc(s.title||d.title||'')}</div><p class="meaning">${esc(s.meaning||d.meaning||'')}</p><div class="meta"><span class="chip">Element: ${esc(s.element||d.element||'—')}</span><span class="chip">Temperament: ${esc(s.temperament||d.temperament||'—')}</span><span class="chip">Bond: ${esc(d.bond??'0')}</span></div>${omen?`<div class="omen"><b>${esc(omen.label)}</b><br>${esc(omen.text)}</div>`:''}`}
window.addEventListener('message',e=>{const m=e.data||{};setBase(m);const d=m.data||{};if(m.action==='close'){hide();return}show();if(m.action==='begin'){content.innerHTML=`<p class="meaning">${esc(locale.beginText||'The circle opens. Smoke rises. Listen.')}</p><div class="meta"><span class="chip">${esc((d.ritual&&d.ritual.label)||d.ritualType||'Ritual')}</span><span class="chip">${esc((d.location&&d.location.label)||'Sacred Ground')}</span></div>`}else if(m.action==='pulse'){app.classList.add('pulse');content.innerHTML=`<p class="meaning">${esc(locale.revealText||'Your spirit has answered.')}</p>`}else if(m.action==='reveal'){content.innerHTML=spiritBlock(d)}else if(m.action==='profile'){content.innerHTML=`<h2>${esc(locale.profile||'Spirit Profile')}</h2>${spiritBlock(d)}`}else if(m.action==='witness'){content.innerHTML=`<p class="meaning">The air shifts. ${esc(d.name)} has awakened the spirit of the ${esc(d.spirit)} nearby.</p>`}else if(m.action==='menu'){const types=d.ritualTypes||{};content.innerHTML=`<p class="meaning">${esc((d.location&&d.location.label)||'Sacred Ground')}</p><div class="menu">${Object.keys(types).map(k=>`<button class="ritualBtn" data-ritual="${esc(k)}">${esc(types[k].label||k)}</button>`).join('')}</div>`;document.querySelectorAll('.ritualBtn').forEach(b=>b.onclick=()=>post('startRitual',{ritualType:b.dataset.ritual}))}});closeBtn.onclick=()=>{hide();post('close')};document.addEventListener('keyup',e=>{if(e.key==='Escape'){hide();post('close')}});
